# URL base del backend (backend-absolute-pos), sin slash final.
# Debe ser HTTPS en producción — un navegador bloquea fetch HTTPS -> HTTP
# (mixed content) y el backend debe tener DASHBOARD_ORIGINS configurado con
# el origen de este dashboard para que CORS lo permita.
VITE_API_URL=http://localhost:3000
